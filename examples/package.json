{
  "name": "@api-zero/examples",
  "private": true,
  "version": "0.1.5",
  "type": "module",
  "description": "Compiled examples rendered by the docs and executed by the playground. Not published.",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "tsc --noEmit && vitest run",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@api-zero/core": "workspace:*",
    "@api-zero/react": "workspace:*",
    "@api-zero/zod": "workspace:*",
    "react": "^19.2.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@tanstack/react-query": "^5.102.6",
    "@testing-library/react": "^16.0.0",
    "@types/react": "^19.2.5",
    "happy-dom": "^15.0.0",
    "swr": "^2.5.1",
    "typescript": "^5.9.3",
    "vitest": "^1.0.0"
  }
}
