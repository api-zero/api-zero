---
"@api-zero/core": patch
---

Documentation only: the published size figures are now measured from the built
bundles rather than estimated, and CI enforces a ceiling on each so they cannot
quietly stop being true.
