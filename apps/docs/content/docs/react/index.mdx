---
title: React
description: One configured client, available anywhere in the tree.
---

import { Boxes, Database, FlaskConical, RefreshCw, Server } from 'lucide-react';

## What this package is for

Every project ends up with the same hand-written `api.ts`: a client wrapper, a
place to stash the auth token, an interceptor or two, and re-declared `.get` /
`.post` helpers. That file is what `@api-zero/react` replaces.

## What it deliberately is not

It is not a data-fetching library. There is no cache, no deduplication, no
invalidation and no `useQuery` equivalent — those belong to
[TanStack Query](https://tanstack.com/query) or [SWR](https://swr.vercel.app),
which solve them far better than a transport layer should try to.

api-zero is the transport and contract layer underneath them. Pairing the two is
the intended setup, not a workaround.

## Installation

```bash
pnpm add @api-zero/react @api-zero/core
```

## Provide the client once

`ApiProvider` accepts either an existing `client` or a `config`. Passing a client
you created yourself is the recommended form: it makes the instance's lifetime
explicit and immune to re-render identity changes.

<include meta='title="app/providers.tsx"'>../../../../../examples/src/react-provider.tsx</include>

## Reach it anywhere with `useApi`

<include meta='title="components/sign-in-button.tsx"'>../../../../../examples/src/react-use-api.tsx</include>

Every component now shares that client, its headers, its interceptors and its
retry policy. No wrapper module, no token plumbing.

## Guides

<Cards>
  <Card
    icon={<Boxes className="text-violet-500" />}
    title="The provider"
    href="/docs/react/guides/the-provider"
    description="One client for the tree, and when to use more than one"
  />
  <Card
    icon={<Database className="text-red-500" />}
    title="TanStack Query"
    href="/docs/react/guides/tanstack-query"
    description="The intended setup, and where retries should live"
  />
  <Card
    icon={<RefreshCw className="text-sky-500" />}
    title="SWR"
    href="/docs/react/guides/swr"
    description="The same pairing, and its cancellation caveat"
  />
  <Card
    icon={<Server className="text-emerald-500" />}
    title="Server rendering"
    href="/docs/react/guides/server-rendering"
    description="Why a shared client leaks tokens between requests"
  />
  <Card
    icon={<FlaskConical className="text-amber-500" />}
    title="Testing"
    href="/docs/react/guides/testing"
    description="Real requests, fake network, nothing to clean up"
  />
</Cards>

## API

| Export | Purpose |
| ------ | ------- |
| [`ApiProvider`](/docs/react/api-reference/api-provider) | Publishes an `ApiClient` to the tree, from a `client` or a `config`. |
| [`useApi()`](/docs/react/api-reference/use-api-hook) | Returns the provided client. Throws outside a provider. |
| `ApiContext` | The underlying context, for advanced composition. |
