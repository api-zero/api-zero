---
title: React bindings
description: One configured client, available anywhere in the tree.
icon: Component
---

import { Anchor, Boxes, Database, FlaskConical, RefreshCw, Server } from 'lucide-react';

Every React project ends up with the same file: a client wrapper, a place to
stash the auth token, an interceptor for 401s, and re-declared `.get` / `.post`
helpers. It is called `api.ts` or `useApi.ts`, it is subtly different in every
project, and nobody ever tests it.

`@api-zero/react` is that file, written once.

## What it is not

<Callout type="warn">
  **This is not a data-fetching library.** There is no cache, no deduplication,
  no invalidation and no `useQuery` equivalent.
</Callout>

Those belong to [TanStack Query](https://tanstack.com/query) or
[SWR](https://swr.vercel.app), which solve them far better than a transport
layer should try to. api-zero sits **underneath** them, and pairing the two is
the intended setup rather than a workaround.

The package used to ship `useRequest` and `useMutation`. They were removed
before the first release — see
[ADR-003](https://github.com/api-zero/api-zero/blob/main/docs/adr/003-scope-boundary-and-public-surface.md)
for why, including the pagination bug that came from building a query library by
accident.

## Installation

`@api-zero/core` is a peer dependency, so install both.

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']} defaultIndex={1}>

  <Tab value="npm">

    ```bash
    npm install @api-zero/core @api-zero/react
    ```

  </Tab>

  <Tab value="pnpm">

    ```bash
    pnpm add @api-zero/core @api-zero/react
    ```

  </Tab>

  <Tab value="yarn">

    ```bash
    yarn add @api-zero/core @api-zero/react
    ```

  </Tab>

  <Tab value="bun">

    ```bash
    bun add @api-zero/core @api-zero/react
    ```

  </Tab>

</Tabs>

<Callout type="info">
  React 18 and 19 are both supported. The peer range is `react >= 18`, so the
  package never pins a version for you.
</Callout>

## Setup

<Steps>

<Step>

### Create the client

Outside the component tree, so its identity never changes across renders.

<include meta='title="app/providers.tsx"'>../../../../../examples/src/react-provider.tsx</include>

</Step>

<Step>

### Reach it from any component

<include meta='title="components/sign-in-button.tsx"'>../../../../../examples/src/react-use-api.tsx</include>

Every component now shares that client, its headers, its interceptors and its
retry policy. Setting a token in one is visible to all of them.

</Step>

<Step>

### Add a cache

api-zero deliberately stops here. Pair it with TanStack Query or SWR, and
forward the `signal` so cancellation reaches the real request.

<include meta='title="hooks/use-user.ts"'>../../../../../examples/src/react-tanstack.tsx</include>

</Step>

</Steps>

## Guides

<Cards>
  <Card
    icon={<Boxes className="text-violet-500" />}
    title="The provider"
    href="/docs/react/guides/the-provider"
    description="One client for the tree, and when to use more than one"
  />
  <Card
    icon={<Database className="text-red-500" />}
    title="TanStack Query"
    href="/docs/react/guides/tanstack-query"
    description="The intended setup, and where retries should live"
  />
  <Card
    icon={<RefreshCw className="text-sky-500" />}
    title="SWR"
    href="/docs/react/guides/swr"
    description="The same pairing, and its cancellation caveat"
  />
  <Card
    icon={<Server className="text-emerald-500" />}
    title="Server rendering"
    href="/docs/react/guides/server-rendering"
    description="Why a shared client leaks tokens between requests"
  />
  <Card
    icon={<FlaskConical className="text-amber-500" />}
    title="Testing"
    href="/docs/react/guides/testing"
    description="Real requests, fake network, nothing to clean up"
  />
</Cards>

## Reference

<Cards>
  <Card
    icon={<Boxes className="text-violet-500" />}
    title="ApiProvider"
    href="/docs/react/api-reference/api-provider"
    description="Publishes a client to the tree"
  />
  <Card
    icon={<Anchor className="text-cyan-500" />}
    title="useApi"
    href="/docs/react/api-reference/use-api-hook"
    description="Returns the provided client"
  />
</Cards>

`ApiContext` is also exported, for composition beyond what `useApi` covers.
