---
title: Getting started
description: Your first request, and what happens when it fails.
icon: SwatchBook
---

Every code block on this page is compiled against the published packages on
every CI run. If an example stops matching the API, the build fails before the
page can mislead you.

<Steps>

## Create a client

<include>../../../../../examples/src/quick-start.ts#create</include>

Export this from a shared module. One instance carries your base URL, headers,
auth and retry policy for the whole application.

## Make a request

<include>../../../../../examples/src/quick-start.ts#verbs</include>

The type parameter describes the response body. It is a static promise, not a
runtime check — for payloads you do not control, add
[`@api-zero/zod`](/docs/zod) and have it verified for real.

## Handle failure

<include>../../../../../examples/src/errors.ts#handling</include>

Everything that can go wrong arrives as an `ApiError`: HTTP status, network
failure, timeout, caller cancellation and invalid JSON alike.

</Steps>

## Query parameters

<include>../../../../../examples/src/quick-start.ts#params</include>

Arrays can be serialized as `brackets`, `repeat` or `comma` through
`paramsSerializer`, per client or per request.

## Authentication

Set it once on the client; every later request carries it.

<include>../../../../../examples/src/auth.ts#bearer</include>

Basic auth and arbitrary headers work the same way:

<include>../../../../../examples/src/auth.ts#headers</include>

## What an error carries

An `ApiError` is not just a message. It keeps the context you need to decide
what to do next.

<include>../../../../../examples/src/errors.ts#context</include>

`isTimeout` and `isAborted` stay distinct on purpose: a request the server was
too slow to answer and a request your own code cancelled deserve different
handling, and most clients collapse them into one.

## Cancellation

<include>../../../../../examples/src/cancellation.ts#abort</include>

Timeouts compose with caller cancellation into a single signal, and the
resulting error tells you which one fired.

## Next steps

<Cards>
  <Card
    title="Concepts"
    href="/docs/core/concepts"
    description="The request lifecycle and the retry policy"
  />
  <Card
    title="Retries"
    href="/docs/core/api-reference/retry"
    description="Backoff, jitter and Retry-After"
  />
  <Card
    title="Zod contracts"
    href="/docs/zod"
    description="Validate payloads at runtime with inferred types"
  />
  <Card
    title="React"
    href="/docs/react"
    description="One client for the whole tree, paired with TanStack Query"
  />
</Cards>
