---
title: Examples
description: Complete patterns, assembled from the pieces.
---

Every snippet on this page is a real file in the repository, compiled against
the published packages on every CI run. If one stops matching the API, the build
fails before the page can mislead you.

## The shared client

Create it once, export it, import it everywhere. This single decision is most of
what api-zero is for.

<include>../../../../../examples/src/create-client.ts</include>

## CRUD

<include>../../../../../examples/src/http-verbs.ts</include>

Each verb returns the parsed body. There is no envelope to unwrap.

## Failure handling worth shipping

<include>../../../../../examples/src/error-handling.ts</include>

A `404` is often not an error for the caller — a missing resource is an answer.
Distinguishing it from a network failure is the difference between an empty state
and an error screen.

## Retries for a flaky upstream

<include>../../../../../examples/src/retry-config.ts</include>

## Authentication that survives an expired token

<include>../../../../../examples/src/auth-refresh.ts</include>

## React with TanStack Query

The intended React setup. TanStack owns the cache and the server-state
lifecycle; api-zero owns the request, the errors and the contract.

<include>../../../../../examples/src/react-provider.tsx</include>

<include>../../../../../examples/src/react-tanstack.tsx</include>

Forwarding `signal` is the detail that matters. Without it, a cancelled query
stops updating React but the HTTP request keeps running to completion — you pay
for the response and throw it away.

## Runtime-validated contracts

<include>../../../../../examples/src/zod-typed-response.ts</include>

The schema produces both the validation and the type, so they cannot disagree.

## Upload with progress

<include>../../../../../examples/src/upload-progress.ts</include>

## A test with no network

<include>../../../../../examples/src/custom-transport.ts</include>

Replacing `transport` replaces the entire network boundary — no global patching,
no local server, no flakiness.

## See also

<Cards>
  <Card
    title="Getting started"
    href="/docs/core/getting-started"
    description="The same pieces, explained one at a time"
  />
  <Card
    title="Concepts"
    href="/docs/core/concepts"
    description="The lifecycle these examples run through"
  />
</Cards>
